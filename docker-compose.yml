services:
  app:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "4040:3000"
    environment:
      - NODE_ENV=production
    command: sh -c "npm install && npm run build && npm run start"
    restart: unless-stopped